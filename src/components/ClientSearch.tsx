import React from 'react';

interface Props {
  onClientSelect: (client: any) => void;
}

export default function ClientSearch({ onClientSelect }: Props) {
  return (
    <div>
      {/* Client search implementation */}
    </div>
  );
}